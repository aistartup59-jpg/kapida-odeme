# AI Agent Guidance for kapida-odeme

## Purpose
This file tells AI coding agents how to work effectively in this repository.

## Current repository state
- The workspace is currently empty or has no detectable project files.
- No build, test, or documentation files were found at the time this guidance was created.

## How to help
1. If files are added, inspect the root and immediate subdirectories for:
   - `package.json`, `pyproject.toml`, `requirements.txt`, `pom.xml`, or other language-specific manifests
   - source directories like `src/`, `app/`, `lib/`, or `backend/`
   - README or docs files describing install/test commands
2. Ask the user for missing project details when the repository is empty.
3. Avoid making assumptions about language, framework, or deployment.

## When creating or editing files
- Keep changes minimal and directly related to the user request.
- Prefer conventions over custom solutions only when the language/framework is clear.
- If the user asks for a new project setup, ask for the target stack before scaffolding.

## Next steps for agents
- Confirm whether the user wants to initialize a new project here.
- If yes, request the desired language/framework and any existing design details.
- If code appears later, update this guidance with detected tools and commands.
